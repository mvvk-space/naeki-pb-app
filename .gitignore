# dependencies & build output
node_modules/
dist/
out/

# research scratch files (raw dumps, not part of the app)
research_*.txt
menu_images_en.json
branch_links.json

# standalone dev test probes (e.g. .dev-pixel-test.html) — harnesses, not the app
.dev-*.html

# OS / editor noise
.DS_Store
Thumbs.db
*.log
# pocketbase binary + live database (machine-local, re-seedable)
pocketbase
pb_data/

# dev screenshots
shot-*.png

# playwright failure artifacts
test-results/
playwright-report/
